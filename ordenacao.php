<?php

    // Encontrar o maior valor em um conjunto de numeros de uma array.
    // e exibir esse valor ao final para o usuário.

    $nums = [ 10, 80, 5, 14, 90, 29, 99, 1];

    // Implementar a logic para encontrar o 99;
    // WHILE || for e IF


    $maiorNumero = $nums;

    while ($maiorNumero < count($nums));
    if ($nums > $maiorNumero);
    $maiorNumero = $num;
    
    echo "O maior número é" ($maiorNumero);

    por meio da tecnica de ordenacao bubble sort
    ordenar array em ordem crescente
